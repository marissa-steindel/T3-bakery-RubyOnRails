<%= form_with url: search_results_products_path, method: :get do |form| %>
  <%= form.text_field :query, :required => true %>
  <%# form.select :category, ["Fiction", "Non-Fiction"] %>
  <%= form.select :category, options_from_collection_for_select(Category.all, 'id', 'name'), include_blank: true %>
  <%= form.submit "Search" %>
<% end %>